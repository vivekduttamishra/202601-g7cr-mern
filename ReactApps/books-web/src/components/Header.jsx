import {useState} from 'react';
import Navigator from './Navigator';

const Header = ({id}) => {

    return (
        <div className='Header '>
            <Navigator />
            
        </div>
    );
};

export default Header;